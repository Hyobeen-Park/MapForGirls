<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mapforgirls">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_applogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_applogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapForGirls">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDI_qpv6fN1MOybVyooVDSzFo3ukcARXgw" />

        <!-- android:name=".MainActivity" -->
        <!-- <activity android:name="com.example.mapforgirls.SplashActivity"
            android:exported="true"> -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name=".ui.onboarding.OnboardingActivity"/>-->
        <activity android:name=".MainActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".FindpwActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".CertifyActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ui.main.columns.ColumnDetailActivity" />
        <activity android:name=".ui.main.chatting.ChattingDetailActivity"/>

    </application>

</manifest>